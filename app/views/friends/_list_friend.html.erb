<li class="<%= "following" if following %>" id="<%= "friend_#{friend.id}" %>" data-friend-id="<%= friend.id %>" data-relationship-id="<%= following.id unless following.blank? %>">
  <% if friend.has_shared_list? %>
    <%= link_to public_list_path(friend.slug), target: "_blank" do %>
      <%= image_tag friend.profile_picture.friend, alt: friend.display_name %>
    <% end -%>
  <% else -%>
    <%= image_tag friend.profile_picture.friend, alt: friend.display_name %>
  <% end -%>
  <div class="friend-info">
    <% if friend.has_shared_list? %>
      <%= link_to public_list_path(friend.slug), target: "_blank" do %>
        <strong class="mom"><%= friend.display_name %></strong>
      <% end -%>
    <% else -%>
      <strong class="mom"><%= friend.display_name %></strong>
    <% end -%>
    <% if friend.list.present? && friend.list.expert? -%>
      <span class="ss-icon ss-target expert" title="Expert list"><em>Expert</em></span>
    <% end -%>
    <div class="friend-data">
      <div class="friend-followers"><%= follower_count(friend) %></div>
      <% if friend.has_shared_list? %>
        <div class="friend-listitems"><%= list_items_count(friend)%></div>
        <div class="friend-updated">Updated list <strong><%= list_updated_at(friend)%></strong></div>
      <% end -%>
    </div>
    <!-- expandable profile text -->
    <div class="profile expandable">
      <p>Has a 2 1/2 year old son. I gravitate towards organic and wholesome parenting type products. I am willing to spend a little more for quality, but have a hard time spending a lot unless I'm really excited about something. I am also a huge fan of finding great baby gear at consignment or thrift stores (especially clothes!) and love a good bargain. I also have a slight addiction to amazon.com. I order way too many items late night on my phone (do not get the amazon iphone app unless you are ready to impulse buy!).</p>
    </div>
    <!-- end expandable profile text -->
    <% if following -%>
      <em class="ss-icon check">checked</em>
    <% end -%>
    <% if current_user.present? && !current_user.guest? %>
      <% if following -%>
        <!-- button: state 1: Follow, state 2: Following, state 2 hover: Unfollow  -->
        <button class="button bt-color bt-follow bt-active unfollow" type="submit">
          <span class="bt-text1"><span class="ss-check"></span>Following</span><em class="bt-text2"><span class="ss-delete"></span>Unfollow</em>
        </button>
      <% else -%>
        <button class="button bt-color bt-follow follow" type="submit"><span class="ss-plus"></span><%= follow_text %></button>
      <% end -%>
    <% else -%>
      <%= link_to "View", public_list_path(friend.slug), class: "button bt-color bt-follow", target: "_blank" -%>
    <% end -%>
  </div>
</li>
